from app.core.settings import Settings  # Import Settings from settings.py

# Initializes a Singleton instance of Settings and is imported everywhere for consistent configuration.
settings = Settings()